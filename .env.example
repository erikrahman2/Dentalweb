# Copy to .env for local development
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# For Laragon default local MySQL (root with no password):
DATABASE_URL="mysql://root:@127.0.0.1:3306/noer_dental"

# Secret for signing JWT (dev can use any value, production must be long/random)
AUTH_SECRET="dev-secret-change-me"

# Email Service Configuration (untuk OTP dentist)
# Pilih salah satu service dan uncomment yang digunakan

# Option 1: SMTP (Nodemailer)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# Option 2: SendGrid
# SENDGRID_API_KEY="your-sendgrid-api-key"

# Option 3: Resend
# RESEND_API_KEY="your-resend-api-key"

# Email From Address
# EMAIL_FROM="noreply@noerdental.com"
